# Backend Build Tracker

_Source of truth for issue status. Updated at every phase transition._

## Legend
Status: `not started` / `in progress` / `done` / `failed` / `blocked`

| Issue | Title | Round | Worker | Status | Started | Completed | Notes |
|---|---|---|---|---|---|---|---|
| 1 | Single-file ingest → extract → persist | 1 | worker-foundation | done | 2026-07-05 | 2026-07-05 | 17/17 assigned tests pass, verified independently. GraphStore.merge_nodes/remove_file added early (see backend_context.md) |
| 2 | Extend loaders to txt/pdf + paragraph fallback | 2 | worker-file-formats | done* | 2026-07-05 | 2026-07-05 | Production code (loaders.py/chunking.py) verified correct; some test-fixture/report edits were clobbered by a shared-file race, see reconciliation row below |
| 3 | Embed chunks into Chroma | 2 | worker-vector-store | done | 2026-07-05 | 2026-07-05 | Production code + fixtures verified correct (was the "last writer" in the shared-file race, so its own contributions are intact) |
| 16 | Graph read API | 2 | worker-graph-api | done | 2026-07-05 | 2026-07-05 | 4/4 tests pass (route/main.py intact; its conftest.py fixture was clobbered, see reconciliation row) |
| — | Fix: GraphStore.load() drops node `id` on JSON round-trip | 2.5 | worker-graphstore-fix | done | 2026-07-05 | 2026-07-05 | 7/7 test_graph_store.py + 4/4 test_graph_read_endpoint.py pass. Session-limit "failed" notification was a false negative — work completed and verified before the notification fired |
| — | Reconcile Round 2 shared-file clobbering (conftest.py, requirements.txt, backend_context.md, test_ingestion_pipeline.py) | 2.6 | worker-round2-reconcile | done | 2026-07-05 | 2026-07-05 | Restored; independently re-verified by orchestrator: 22/22 (file_loading/chunking/extraction/graph_store) + 12/12 (ingestion_pipeline/vector_store/graph_read_endpoint) pass |
| 4 | Entity resolution — string tier | 3 | worker-entity-resolution | done | 2026-07-05 | 2026-07-05 | 8/8 pass (+1 intentional skip) locally; production code verified, conftest.py fixtures pending merge (row below) |
| 5 | Entity resolution — embedding + LLM tiers | 3 | worker-entity-resolution | done | 2026-07-05 | 2026-07-05 | resolves threshold open question; adjudicate_merge added to openrouter_client.py |
| 9 | Vector-seeded concept lookup | 3 | worker-retrieval-answer | done | 2026-07-05 | 2026-07-05 | 12/12 pass locally; self-caught and fixed a mid-round overwrite of openrouter_client.py by the entity-resolution worker (both workers had to add different functions to the same file — a planning gap, see log) |
| 10 | LLM-guided bounded traversal | 3 | worker-retrieval-answer | done | 2026-07-05 | 2026-07-05 | traversal_next_hop implemented; returns WS-schema-ready step shape |
| 11 | Claude Haiku summary answer | 3 | worker-retrieval-answer | done | 2026-07-05 | 2026-07-05 | generate_answer implemented; query_service.py established as the Round 4/5 extension point |
| — | Merge Round 3 conftest.py fixtures (mock_adjudication_llm, entity_resolution_thresholds, mock_traversal_llm, mock_haiku_client) | 3.5 | worker-round3-conftest-merge | done | 2026-07-05 | 2026-07-05 | Independently re-verified by orchestrator: 54 passed, 1 skipped (intentional) across full Round 1-3 owned test scope |
| 6 | Folder watcher + hash diff | 4 | worker-watcher | done | 2026-07-05 | 2026-07-06 | Worker interrupted by session-limit reset before copy-back/report; orchestrator independently inspected its worktree, verified hash_store.py/watcher.py/startup.py were complete and correct, copied them + the fully-implemented test file into the shared checkout, added watchdog to requirements.txt, and ran the test suite directly |
| 7 | File deletion cleanup | 4 | worker-watcher | done | 2026-07-05 | 2026-07-06 | 6/6 tests pass in test_incremental_ingestion.py, verified directly by orchestrator |
| 8 | Startup load-then-diff-scan | 4 | worker-watcher | done | 2026-07-05 | 2026-07-06 | startup() returns (graph_store, thread) so callers can assert immediate availability then .join() to await reconciliation deterministically |
| 12 | No-match detection | 4 | worker-no-match-chat | done | 2026-07-05 | 2026-07-06 | Worker's own completion message was garbled/no report written (session-limit artifact) — orchestrator independently verified all production code (detector.py, judge_relevance, query_service.py, conftest.py mock_haiku_client extension) and ran all 15 owned tests directly: 15/15 pass, including Round 3's regression test unmodified |
| 13 | Chat session 5-turn window | 4 | worker-no-match-chat | done | 2026-07-05 | 2026-07-06 | resolves chat-persistence open question (final, in-memory only); xfail correctly removed from test_session_state_does_not_survive_backend_restart |
| 14 | WS traversal streaming | 5a | worker-ws-streaming | in progress | 2026-07-06 | | agent a72b3413e12e7b954 — resolves WS schema open question |
| 15 | Folder configuration API | 5a | worker-folder-config | in progress | 2026-07-06 | | agent a882a657eea04630f — resolves API shape open question; both 5a workers add one line each to main.py via targeted Edit |
| 17 | Concurrency lock guarding | 5b | worker-concurrency | not started | | | Sequenced AFTER 5a (needs final query_service.py from Issue 14, watcher.py/startup.py from Round 4) |
| — | Integration + full test suite | 6 | worker-integration | not started | | | Gate for SHIPPED |

## Round status

- Round 1: done
- Round 2: done
- Round 3: done
- Round 4: done
- Round 5: in progress
- Round 6 (integration): not started

## Log

- 2026-07-05: Planning complete. orchestrator_plan.md and backend_context.md written. Starting Round 1.
- 2026-07-05: Round 1 (Issue 1) complete. worker-foundation built backend/config.py, main.py, clients/, ingestion/{loaders,chunking,pipeline}.py, extraction/extractor.py, graph_store/store.py, requirements.txt. 17/17 assigned tests pass (independently re-verified: `.venv/bin/python3 -m pytest ...` -> 17 passed, 7 errors, all expected NotImplementedError from Issue 2/3 fixtures). Key notes for later rounds: FastAPI app lives at backend/main.py (not backend/api/main.py); GraphStore already has merge_nodes() and remove_file() for Issues 4/5/6/7 to call; GRAPH_STORE_PATH in config.py (default ./graph_store.json). Starting Round 2 (Issues 2, 3, 16 in parallel).
- 2026-07-05: Round 2 worker-graph-api (Issue 16) done, 4/4 tests pass. Surfaced a real bug: GraphStore.load() drops each node's `id` data attribute on JSON round-trip (networkx node_link_data/node_link_graph reserve "id" as the node-key field). Worked around locally in graph_routes.py, but this would bite every future round reading a reloaded graph's node id (entity resolution, watcher, retrieval) if left unfixed at the source. Dispatched a small out-of-band remediation worker (worker-graphstore-fix) to patch GraphStore.load() centrally before Round 3 starts. worker-file-formats and worker-vector-store still in progress.
- 2026-07-05: Session-limit reset occurred mid-round, producing "failed" notifications for worker-file-formats, worker-vector-store, and worker-graphstore-fix. Investigation showed all three actually completed their core work before the notification fired (false-negative failures). However, running the full test suite surfaced a real, separate problem: `docs/backend/tests/conftest.py`, `requirements.txt`, `backend_context.md`, and `docs/backend/tests/integration/test_ingestion_pipeline.py` are each shared across 2+ Round 2 workers, and since workers copy whole files into/out of isolated worktrees (no git merge — no commits exist on any worktree branch), the LAST worker to write back a shared file silently discarded the others' edits to that same file. worker-vector-store wrote back last, so its own conftest.py/requirements.txt/backend_context.md/test_ingestion_pipeline.py edits survived, but worker-file-formats' (sample_txt_file, sample_pdf_file, sample_pdf_file_no_headings fixtures; pdfplumber/fpdf2 in requirements.txt; PDF library choice section; test_mixed_format_folder_ingests_correctly body) and worker-graph-api's (fastapi_test_client fixture) edits to those SAME shared files were lost. All non-shared production code files were verified unaffected (each lives in a file only one Round-2 worker touched). Diagnosed precisely via direct file reads + isolated worktree inspection (no worktree had committed history to recover from, so lost fixture bodies had to be reconstructed from worker reports' descriptions, except `fastapi_test_client` which was recoverable byte-for-byte from worker-graph-api's still-present worktree copy). Dispatched worker-round2-reconcile with an exact restore-list. **Process fix for Round 3 onward:** any shared file (conftest.py, requirements.txt, backend_context.md) touched by 2+ parallel workers in a round will now be reconciled in one dedicated serial step after those workers finish their non-overlapping production-code work, instead of trusting parallel write-backs.
- 2026-07-05: Attempted to `git commit` a checkpoint of the verified-correct production code as a safety net; blocked because no git user.name/email is configured in this environment and the orchestrator is prohibited from changing git config. Proceeding without git-based checkpointing — relying on direct pytest re-verification after every worker instead (already standard practice this build).
- 2026-07-05: worker-round2-reconcile complete. Restored sample_txt_file/sample_pdf_file/sample_pdf_file_no_headings/fastapi_test_client fixtures, pdfplumber+fpdf2 in requirements.txt, PDF library choice section in backend_context.md, and test_mixed_format_folder_ingests_correctly body. Independently re-verified by orchestrator (not just trusting the report): `test_file_loading.py`+`test_chunking.py`+`test_extraction.py`+`test_graph_store.py` -> 22/22 pass; `test_ingestion_pipeline.py`+`test_vector_store.py`+`test_graph_read_endpoint.py` -> 12/12 pass. Round 2 fully done. Starting Round 3 (Issues 4+5 entity resolution, Issues 9+10+11 retrieval+traversal+answer, in parallel).
- 2026-07-05: Both Round 3 workers completed successfully, correctly avoiding writing conftest.py back to the shared checkout (each verified 8-12 owned tests passing locally against a scratch conftest.py copy). One new hazard surfaced mid-round: I had assigned BOTH workers to add a different function to the SAME file, `backend/clients/openrouter_client.py` (entity-resolution added `adjudicate_merge`, retrieval-answer added `traversal_next_hop`) — the same whole-file-overwrite risk as conftest.py, which my Round 2 process fix didn't anticipate extending to shared *production* files, not just test infra. worker-retrieval-answer caught this itself (found its own `traversal_next_hop` implementation had been silently reverted by the entity-resolution worker's later whole-file write), re-applied its patch surgically preserving the other worker's addition, and re-verified all 12 of its own tests still pass. Independently verified by the orchestrator: read the final `openrouter_client.py` and confirmed all four functions (`extract_concepts`, `embed_text`, `adjudicate_merge`, `traversal_next_hop`) are present and correct. **Process fix, round 4 onward:** treat any file named in 2+ parallel workers' briefs (not just conftest.py/requirements.txt/backend_context.md, but any shared production file like a clients module) with the same "verify-locally, report-verbatim-code, merge-serially" discipline, or explicitly sequence those workers instead of parallelizing them. Dispatched worker-round3-conftest-merge to apply the four reported fixture implementations (mock_adjudication_llm, entity_resolution_thresholds, mock_traversal_llm, mock_haiku_client) to the shared conftest.py in one serial pass.
- 2026-07-05: worker-round3-conftest-merge complete, applied all 4 fixtures verbatim as specified. Independently re-verified by orchestrator (ran the full combined command myself, not just trusting the report): 54 passed, 1 skipped (the intentional `test_threshold_boundary_behavior_is_parameterized_not_hardcoded`), exactly matching the report. Round 3 fully done. Starting Round 4 (Issues 6+7+8 watcher/cleanup/startup, Issues 12+13 no-match/chat-session, in parallel).
- 2026-07-06: A session-limit reset interrupted both Round 4 workers mid-task, producing garbled/incomplete completion signals with no reports written for either. Neither is trustworthy on its own — both were independently verified directly: (1) worker-no-match-chat's production code (backend/no_match_detection/, backend/chat_session/, judge_relevance, query_service.py's new signature, conftest.py's extended mock_haiku_client) was inspected file-by-file and all 15 owned tests were run directly by the orchestrator: 15/15 pass, including confirmation that Round 3's `test_relevant_query_returns_grounded_four_to_five_line_answer_end_to_end` still passes completely unmodified. (2) worker-watcher never copied its finished worktree files back to the shared checkout at all — orchestrator located them in `/workspace/.claude/worktrees/agent-a861b54d002c7d478/backend/ingestion/`, read and verified `hash_store.py`/`watcher.py`/`startup.py` were complete and correct against the brief, confirmed the worktree's stale `conftest.py` copy (pre-dating the no-match-chat worker's additions) posed no clobbering risk since it was never touched, copied the three production files plus the fully-implemented `test_incremental_ingestion.py` into the shared checkout, added the missing `watchdog` line to `requirements.txt`, and ran the test suite directly: 6/6 pass. Round 4 fully done and independently verified end-to-end. Starting Round 5 (Issue 14 WS streaming, Issue 15 folder config API, Issue 17 concurrency locking, in parallel).
