# Bottom-Docked Chat — Implementation Plan

**Spec:** `docs/superpowers/specs/2026-07-09-chat-dock-design.md` (approved)

### Task 1: Tests first
- Rewrite `tests/integration/ChatPanelCollapse.test.tsx` → `tests/integration/ChatDockToggle.test.tsx`: collapsed by default (body hidden, bar visible); bar click expands, second click collapses; transcript + scrollTop survive a cycle; traversal keeps flowing while collapsed; the mocked WebSocket is constructed exactly once across toggles; connection chip present in both states.
- Update `tests/unit/AppShell.test.tsx`: row contains folder + graph only; `chat-dock` present with `fixed`/`bottom-0` classes; graph keeps `flex-1`.

### Task 2: Implement
- New `src/components/chat/ChatDock.tsx`: fixed bottom-right container; header bar (ConnectionStatusChip + "Chat" + chevron) as the toggle button (`aria-expanded`, labels "Expand chat"/"Collapse chat"); body (`ChatTranscript` + `ChatInput`) CSS-hidden when collapsed, never unmounted; bar w-80 collapsed, window w-96 (capped to viewport) × ~70vh expanded.
- `App.tsx`: drop ChatPanel from the row; render `<ChatDock />` as a sibling overlay. Delete `ChatPanel.tsx`; fix stale comment references in `ChatTranscript.tsx`/`useTraversalSync.ts`.

### Task 3: Verify
- Full vitest suite (native copy), `tsc -b`, context-doc entry.
